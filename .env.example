# Firebase Configuration (Already configured in src/firebase.js)
# For Vercel deployment:
# 1. No environment variables needed - Firebase config is hardcoded
# 2. Make sure your Firebase project accepts anonymous authentication
# 3. Enable Firestore in your Firebase project

# Optional: Add rate limiting for production
# VITE_API_RATE_LIMIT=100
